T1:

https://gist.github.com/zachbrowne/8bc414c9f30192067831fafebd14255c#file-the-ultimate-bad-ass-bashrc-file-L302
cpg ()
// This copies a file or directory to $2. First it checks if $2 is actually a directory. If it is, it copies the file in and then cds into that directory right after (the && just means it only cds if the copy actually worked). If $2 isn't a directory, like if you're just renaming something, it skips the cd and just does a normal copy.

https://gist.github.com/zachbrowne/8bc414c9f30192067831fafebd14255c#file-the-ultimate-bad-ass-bashrc-file-L329
up ()
// Lets you jump up multiple folders at once instead of typing out cd ../../.. every time, so "up 3" does the same thing. It takes the number you give it and builds a string of "/.." that many times using a for loop, then cleans up the extra slash at the start with sed. If somehow nothing gets built, it just defaults to going up one folder.

T2:

https://github.com/DivsheenKaur13/cs131/blob/544f6f461461a738b326d0b2bf2824fa94fd7613/ws4/.bashrc#L2
alias cs131='cd ~/cs131'
// Shortcut so I don't have to type cd ~/cs131 every single time I want to get back to my repo.

https://github.com/DivsheenKaur13/cs131/blob/544f6f461461a738b326d0b2bf2824fa94fd7613/ws4/.bashrc#L5
alias gl='git log --oneline --graph --decorate'
// Makes git log way easier to read since it shows everything on one line with a little branch graph instead of the long default output.

https://github.com/DivsheenKaur13/cs131/blob/544f6f461461a738b326d0b2bf2824fa94fd7613/ws4/.bashrc#L8
mkws ()
// This helps me make a new worksheet folder without accidentally overwriting one I already have. If the folder name already exists it just tells me instead of doing anything, and if it doesn't exist yet it creates it and moves me into it.

https://github.com/DivsheenKaur13/cs131/blob/544f6f461461a738b326d0b2bf2824fa94fd7613/ws4/.bashrc#L10
if [ -d "$1" ]; then
// This is the part of mkws that actually checks if the folder exists before deciding what to do next.
